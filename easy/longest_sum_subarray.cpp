#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int longest_subarray(vector<int> &v, int size, int getsum)
{
    
}
int main()
{
    vector<int> v = {10, 5, 2, 7, 1, 9};
    cout<<longest_subarray(v, v.size(),15);
    return 0;
}