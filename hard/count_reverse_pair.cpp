#include<iostream>
#include<vector>
using namespace std;
int count_reverse_pairs(vector<int> &v, int low, int mid, int high)
{
    vector<int> vtemp;
    int i=low, j = mid+1; 

    while(i<=mid && j<=high)
    

}
int main()
{
    vector<int> v = {3,1,2,4,1,5,2,6,4};
    return 0;
}